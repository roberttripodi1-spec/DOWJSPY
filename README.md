# EST Chart Time Patch

This patch converts all chart timestamps to:

- Eastern Time (EST/EDT)
- 12-hour AM/PM format

It also updates:
- chart hover labels
- x-axis timestamps
- joined pattern chart timestamps