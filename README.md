# SPY vs DIA Live Dashboard

Fixed version with:
- Safe close price extraction
- Empty dataframe protection
- RUN LIVE / STOP buttons
- Auto refresh
- Streamlit deployment support

Run:

pip install -r requirements.txt
streamlit run app.py