
# REPLACE THE joined_chart() TRACE COLORS WITH:

fig.add_trace(
    go.Scatter(
        x=normalized.index,
        y=normalized["SPY Pattern"],
        mode="lines",
        name="SPY Pattern",
        line=dict(color="#00cc66", width=3),
        line_shape="spline" if smooth else "linear",
    )
)

fig.add_trace(
    go.Scatter(
        x=normalized.index,
        y=normalized["DIA Pattern"],
        mode="lines",
        name="DIA Pattern",
        line=dict(color="#3399ff", width=3),
        line_shape="spline" if smooth else "linear",
    )
)

# ALSO UPDATE SINGLE CHARTS:

# SPY chart
line=dict(color="#00cc66", width=3)

# DIA chart
line=dict(color="#3399ff", width=3)

# RESULT:
# SPY lines = Green
# DIA/DOW lines = Blue
